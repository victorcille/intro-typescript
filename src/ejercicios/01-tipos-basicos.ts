// Desde la consola, situados en la ruta de nuestro proyecto con el comando 'npm start' levantamos el servidor local para poder trabajar

/*
    ===== Código de TypeScript =====
*/

// Tipos y tipado de variables
let nombre: string = 'Víctor';
let hp: number | string = 95;
hp = 'full';
let estaVivo: boolean = true;
